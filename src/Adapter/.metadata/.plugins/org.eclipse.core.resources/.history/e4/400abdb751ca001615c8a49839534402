/*
 * timer.h
 *
 *  Created on: 2016年12月22日
 *      Author:Topon-Edison
 */

#ifndef DRIVERS_INC_TIMER_H_
#define DRIVERS_INC_TIMER_H_


/*
 *@brief Definition to distinguish the timer channel of MRT,
 *       Timre0 is the channel zero and so on.
 */
typedef enum TimerNum
{
	Timer0 = 0,
	Timer1,
	Tiemr2,
}Timer_T;

// The Declaration of the MRT timer type
struct LPC_MRT_CH_T;

class Timer
{
public :
   Timer* Instance(Timer_T );
   void Start(uint32_t interval);
   void Stop();
   bool IsExpired();

private:
   Timer(Timer_T);
   LPC_MRT_CH_T * timer_;
};






#endif /* DRIVERS_INC_TIMER_H_ */
